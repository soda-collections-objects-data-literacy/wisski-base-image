name: Publish WissKI Base Docker Images

on:
  push:
    branches: [2.x, 3.x, 4.x]
    tags:
      - "*.*.*"

env:
  REGISTRY: ghcr.io
  PLATFORMS: linux/amd64,linux/arm64
  CONTEXT_DIRECTORY: .

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    strategy:
      matrix:
        mode: [production, development]
        include:
          - mode: production
            image_name: soda-collections-objects-data-literacy/wisski-base-image-production
          - mode: development
            image_name: soda-collections-objects-data-literacy/wisski-base-image-development
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Determine if latest release tag
        id: release
        run: |
          is_latest=false
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            current="${GITHUB_REF#refs/tags/}"
            latest=$(git tag -l '*.*.*' --sort=-v:refname | head -1)
            if [[ -n "$latest" && "$current" == "$latest" ]]; then
              is_latest=true
            fi
          fi
          echo "is_latest=$is_latest" >> "$GITHUB_OUTPUT"
      - uses: docker/metadata-action@v6
        id: meta
        with:
          images: |
            ${{ env.REGISTRY }}/${{ matrix.image_name }}
          tags: |
            type=raw,value=latest,enable=${{ steps.release.outputs.is_latest == 'true' }}
            type=raw,value=${{ github.ref_name }},enable=${{ startsWith(github.ref, 'refs/heads/') }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=sha
      - uses: docker/setup-qemu-action@v4
      - uses: docker/setup-buildx-action@v4
      - uses: docker/login-action@v4
        with:
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          registry: ${{ env.REGISTRY }}
      # Development package line and production package version come from the
      # Dockerfile ARG defaults (WISSKI_PACKAGES_LINE / WISSKI_PACKAGES_VERSION).
      # Do not derive them from the git tag major — image major can diverge
      # from the drupal_packages line (e.g. image 4.x still baking packages 3.x).
      - uses: docker/build-push-action@v7
        with:
          context: ${{ env.CONTEXT_DIRECTORY }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            MODE=${{ matrix.mode }}
          platforms: ${{ env.PLATFORMS }}
          pull: true
          push: true
          provenance: false
